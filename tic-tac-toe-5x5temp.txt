#include <stdio.h>
#include <stdlib.h>

int rol_check(int, int);
int col_check(int, int);
int x_check(int, int);

const int size = 5;

int main(){
	int n, k;
    int c[size], r[size],x[2];
    int matrix[size][size];
    int nothing=0;
    FILE *fPtr1, *fPtr2;

    fPtr1 = freopen("moveIn.txt","r",stdin);
    //fPtr2 = freopen("movesOut.txt","w",stdout);
    if (!fPtr1) {
        printf("檔案建立失敗...\n");
        exit(1);
    }
    /*
	if (!fPtr2) {
        printf("檔案建立失敗...\n");
        exit(1);
    }
	*/
	scanf("%d", &n);
	printf("%d round(s)\n", n);
	
	for(k=0; k<n; k++)
	{
		printf("Test Case #%d:\n", k+1);
		
		for(int row=0; row<size; row++)
			for(int col=0; col<size; col++)
				scanf("%c", &matrix[row][col]);
		for(int row=0; row<size; row++)
			for(int col=0; col<size; col++)
				printf("%c", matrix[row][col]);
			
		
		
		printf("\n");
	}
	
	system("pause");
    return 0;
}